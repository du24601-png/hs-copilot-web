# 独立判例层实施记录

按用户已批准方案执行：只导入清洗版104条；27条唯一前缀候选、77条多候选均待业务确认。普通归类保留三次LLM调用，判例只提供证据与现行候选，不覆盖现行税则或编码白名单。

## 验收清单

- [x] 创建 codex/ruling-knowledge-integration 分支，保留原有文档删除与两个PDF。
- [x] 现有Node测试基线35/35通过。
- [x] 独立判例表、只读预检、备份、原子导入与回滚测试。104条、27/77、452映射；原有30表指纹一致。
- [x] 判例检索与候选补充、上下文和引用白名单测试。
- [x] classify/decide接入、缓存版本、三次调用与降级测试。重召回为空明确拒答，不沿用旧依据。
- [x] 参考判例页面、旧历史兼容与浏览器验收。Edge桌面1440×1000、手机390×844通过。
- [x] Python与Node全量测试通过后执行80个流程（两集×关闭/开启×20）。
- [x] 保存四份结果与对照报告，按安全门槛保持默认关闭并更新文档。

## 固定测试约束

出海电商只回传原商品事实，不读取标准答案或选项编码来作答；疑难集空答案。并发2、请求超时120秒、相同代码/数据库/模型配置、每臂全新进程。20例疑难中14例对应判例入库、6例未入库，分组报告。只跑约定轮次，禁止改标签、补回已删除判例或挑最好成绩。

如果精确正确数下降、接口错误或对照条件不一致，默认保持关闭，交付逐例失败信息。

## 已验证证据

- 功能安全测试：Node 57/57；Python 25/25（5个规则导入、20个判例导入测试）。在线测试前全部通过。
- 导入备份：`tools/backup/hs_copilot-before-rulings-20260904T015127.187971Z-7d69e70d.db`（UTC文件名）。
- 原始清洗工作簿：`tools/data/rulings-104.xlsx`；SHA-256 `80adab77952f020a8851961ed30a4e38c3254a2e4f61acc92b7aa27f1d602913`。
- 93条发布日期为空、24条决定叙述为空均保留；104条有效性未经业务确认，452条映射全部pending。
- 规格审核与代码质量审核通过。补强了逐臂遥测/判例降级门槛、数据集字节与哈希绑定、英文整词检索和页面模式切换清理。
- 浏览器分析文字使用测试替身，原始判例、来源和税号元数据读取真实数据库；未额外调用在线模型。
- 在线启动第一次在进程创建前被安全审核拦截，核查40条合成/公开描述和模型目的地后复核放行；被拦截启动不计测试轮次。
- 单轮结果：电商16→15；疑难7→15（含4例不可比、1例接口错误）。电商退化与服务异常均阻止默认启用。没有加跑。
- 交付限制：错误/理解失败路径的完整请求计数不可得，报告明确标记可观察下限；不把缺失计数当0成本。
