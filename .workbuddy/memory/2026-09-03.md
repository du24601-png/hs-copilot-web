# 2026-09-03

- 梳理 HS Copilot 查询后台架构（server.js 双路检索 + 两阶段 LLM + 候选校验），产出 docs/08-架构梳理与测试问题分析.md。
- 跑通全部测试：单元 26/26（需 Node 24）；真实 20 例端到端 2/20（10%），拒答 10、错码 8，比上轮 3/20 更差。
- 定位三层根因（均已复现验证）：①检索召回天花板 7/20（词面脱节"淋浴房"0命中；单位词"毫升"带偏 8703；后缀命中即停漏"框架"→7308300000；章节兜底整句 LIKE 必 0 命中；"其他肥料"等兜底税号被品目 6 席上限挤出）②决策层换手 2/7（10 位细分需裁定口径知识，如切碎=粉碎归 0904.22）③LLM 未固定 temperature，结果不可复现。
- 工程小问题：package.json 的 --test-isolation=none 仅 Node 24 支持，Node 22 名为 --experimental-test-isolation。
- 改进优先级建议已写入 docs/08：先固定随机性 → 再修检索清洗与兜底 → 决策层知识注入。
